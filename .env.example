# Google API Key for Gemini
GOOGLE_API_KEY="your-google-api-key-here"

# A JSON string representing a dictionary of allowed API keys and their corresponding user IDs.
# This is a simple authentication mechanism for the demo.
# You can add more keys and users as needed.
API_KEYS='{"test-key": "test-user", "another-secret-key": "user-2"}'

# (Optional) You can override the default paths here if needed.
# CHROMA_DB_PATH="output/custom_chroma_db"
# CHECKPOINT_DIR="output/custom_checkpoints"
